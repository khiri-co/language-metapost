'use babel';

import LanguageMetapostView from '../lib/language-metapost-view';

describe('LanguageMetapostView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
